//1. Declare variáveis que representem os tipos de dados: string, number, boolean e null. 
// Exiba os valores no console.

const texto = 'texto exemplo';
console.log(texto);

const numero = 2024;
console.log(numero);

const sim = true;
console.log(sim);

const vazio = null;
console.log(vazio);

//2. Crie uma variável `aluno` que armazena um valor booleano, e outra `idade` com um valor numérico.
// Exiba as variáveis no console.

const aluno = false;
console.log(aluno);

const idade = 10;
console.log(idade);

//3. Verifique o tipo de dado de cada variável criada usando `typeof`.
console.log(typeof aluno);
console.log(typeof idade);

//4. Converta uma string numérica em um número usando parseInt e exiba o tipo após a conversão.
const numeroTexto = '12';
console.log(parseInt(numeroTexto));

//5. Crie uma variável que armazene um número decimal e outra que armazene uma string; depois, concatene-as e exiba o resultado.
const valorPi = 3.14
const textoPi = 'Pi'
console.log(textoPi + ': ' + valorPi);
